# Rust
target/

# Wrangler yerel durum + gizli değişkenler
.wrangler/
.dev.vars

# NOT: build/ BİLİNÇLİ commit'lidir (prebuilt WASM — fork'layan Rust kurmadan
# deploy eder). Kaynaktan yeniden derlersen build/ güncellenir, commit'le.
node_modules/

# yerel probe/test config'leri
wrangler.probe.toml
wrangler.live.toml
